let rec main foo bar = a[if 28 then new bool]
and f f d = e[+ (match 0.0 with
  false -> "foo"
| (-6) -> 0.0
| 2 -> 'a'
end
)]
and d = delete (match + "Route66" with
  B Cons bar A d -> match true with
  false -> 42.0
end

| Empty A a e (-.42.0) -> ('a')
end
)
and mutable g[match (15 / false) with
  (+.2.56) -> while '\'' do 5 done
end
,new (array [*,*] of array of char)]

