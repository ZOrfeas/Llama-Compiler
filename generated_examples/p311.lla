let rec main bar e = match b "Route66" with
  (-.0.0) -> if 42.0 then 25
end

and mutable e[delete (((Tree "Route66" 'a'))),begin match if 42.0 then true else 9 with
  (-38) -> foo
| true -> (28)
end
 end]
and mutable foo : bool -> char

let a bar : g = dim 2 g
and mutable g

